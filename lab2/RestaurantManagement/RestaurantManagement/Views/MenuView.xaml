<UserControl x:Class="RestaurantManagement.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converters="clr-namespace:RestaurantManagement.Converters"
             xmlns:vm="clr-namespace:RestaurantManagement.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:BoolToStatusConverter x:Key="BoolToStatusConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="➕ Add dish" 
                    Command="{Binding AddMenuItemCommand}"
                    Background="#27AE60"
                    Foreground="White"
                    Padding="15,8"
                    FontWeight="SemiBold"
                    Margin="0,0,10,0"/>
            <Button Content="🗑️ Delete" 
                    Command="{Binding DeleteMenuItemCommand}"
                    Background="#E74C3C"
                    Foreground="White"
                    Padding="15,8"
                    FontWeight="SemiBold"
                    Margin="0,0,10,0"/>
            <Button Content="💾 Save" 
                    Command="{Binding SaveMenuCommand}"
                    Background="#3498DB"
                    Foreground="White"
                    Padding="15,8"
                    FontWeight="SemiBold"/>
        </StackPanel>

        <!-- Menu DataGrid -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding MenuItems}"
                  SelectedItem="{Binding SelectedMenuItem}"
                  Style="{StaticResource DataGridStyle}"
                  ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                  CellStyle="{StaticResource DataGridCellStyle}">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat={}{0} грн}" Width="80"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="100"/>
                <DataGridCheckBoxColumn Header="Avaible" Binding="{Binding IsAvailable}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Details Panel -->
        <Border Grid.Row="2" 
                Background="#F8F9FA" 
                BorderBrush="#DEE2E6" 
                BorderThickness="1" 
                Margin="0,15,0,0"
                Padding="15"
                Visibility="{Binding SelectedMenuItem, Converter={StaticResource NullToVisibilityConverter}}">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Dish details" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Name:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding SelectedMenuItem.Name}" Margin="0,0,0,10"/>

                    <TextBlock Text="Description:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding SelectedMenuItem.Description}" 
                             Height="60" 
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Margin="0,0,0,10"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="Ціна:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding SelectedMenuItem.Price}" Margin="0,0,0,10"/>

                    <TextBlock Text="Category:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <ComboBox SelectedItem="{Binding SelectedMenuItem.Category}"
                             ItemsSource="{Binding Source={x:Static vm:MenuViewModel.Categories}}"
                             Margin="0,0,0,10"/>

                    <CheckBox Content="Avaible for order" 
                             IsChecked="{Binding SelectedMenuItem.IsAvailable}"
                             FontWeight="SemiBold"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>