<UserControl x:Class="RestaurantManagement.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:RestaurantManagement.Controls"
             xmlns:converters="clr-namespace:RestaurantManagement.Converters"
             xmlns:vm="clr-namespace:RestaurantManagement.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:BoolToStatusConverter x:Key="BoolToStatusConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="➕ Add order" 
                    Command="{Binding AddOrderCommand}"
                    Background="#27AE60"
                    Foreground="White"
                    Padding="15,8"
                    FontWeight="SemiBold"
                    Margin="0,0,10,0"/>
            <Button Content="🗑️ Delete" 
                    Command="{Binding DeleteOrderCommand}"
                    Background="#E74C3C"
                    Foreground="White"
                    Padding="15,8"
                    FontWeight="SemiBold"
                    Margin="0,0,10,0"/>
            <Button Content="💾 Save" 
                    Command="{Binding SaveOrdersCommand}"
                    Background="#3498DB"
                    Foreground="White"
                    Padding="15,8"
                    FontWeight="SemiBold"/>
        </StackPanel>

        <!-- Orders DataGrid -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Orders}"
                  SelectedItem="{Binding SelectedOrder}"
                  Style="{StaticResource DataGridStyle}"
                  ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                  CellStyle="{StaticResource DataGridCellStyle}">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Client" Binding="{Binding CustomerName}" Width="150"/>
                <DataGridTextColumn Header="Date" Binding="{Binding OrderDate, StringFormat=dd/MM/yyyy HH:mm}" Width="120"/>
                <DataGridTextColumn Header="Sum" Binding="{Binding TotalAmount, StringFormat={}{0} грн}" Width="100"/>
                <DataGridTemplateColumn Header="Status" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <controls:StatusIndicator Status="{Binding Status}" 
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Details Panel -->
        <Border Grid.Row="2" 
                Background="#F8F9FA" 
                BorderBrush="#DEE2E6" 
                BorderThickness="1" 
                Margin="0,15,0,0"
                Padding="15"
                Visibility="{Binding SelectedOrder, Converter={StaticResource NullToVisibilityConverter}}">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Order details" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Client:" FontWeight="SemiBold" Margin="0,0,10,5"/>
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SelectedOrder.CustomerName}" Margin="0,0,20,5"/>

                        <TextBlock Grid.Row="0" Grid.Column="2" Text="Sum:" FontWeight="SemiBold" Margin="0,0,10,5"/>
                        <TextBox Grid.Row="0" Grid.Column="3" Text="{Binding SelectedOrder.TotalAmount}" Margin="0,0,0,5"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Status:" FontWeight="SemiBold" Margin="0,0,10,5"/>
                        <ComboBox Grid.Row="1" Grid.Column="1" 
                                 SelectedItem="{Binding SelectedOrder.Status, Mode=TwoWay}"
                                 ItemsSource="{Binding Source={x:Static vm:OrdersViewModel.OrderStatuses}}"
                                 Margin="0,0,20,5"/>

                        <TextBlock Grid.Row="1" Grid.Column="2" Text="Date:" FontWeight="SemiBold" Margin="0,0,10,5"/>
                        <DatePicker Grid.Row="1" Grid.Column="3" 
                                   SelectedDate="{Binding SelectedOrder.OrderDate}"
                                   Margin="0,0,0,5"/>
                    </Grid>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>