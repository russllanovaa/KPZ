<UserControl x:Class="RestaurantManagement.Controls.StatusIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="100">
    <UserControl.Resources>
        <Storyboard x:Key="ColorChangeAnimation" x:Shared="False">
            <ColorAnimation 
                Storyboard.TargetName="StatusBorder"
                Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                Duration="0:0:0.5" />
        </Storyboard>
    </UserControl.Resources>
    <Border x:Name="StatusBorder" 
            CornerRadius="4" 
            Padding="8,4"
            Background="{Binding StatusColor, RelativeSource={RelativeSource AncestorType=UserControl}}">
        <TextBlock x:Name="StatusTextBlock" 
                   Text="{Binding StatusText, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                   Foreground="White" 
                   FontWeight="SemiBold" 
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>
    </Border>
</UserControl>